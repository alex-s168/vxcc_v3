name: Analyze code

on: [push]

jobs:
  run_tests:
    runs-on: self-hosted,ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
      - name: Analyze
        run: |
          chmod +x build.sh
          ./build.sh analyze
        shell: bash
      - name: Try compile
        run: |
          chmod +x build.sh
          ./build.sh
        shell: bash
